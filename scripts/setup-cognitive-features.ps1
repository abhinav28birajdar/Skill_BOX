# Install core dependencies
npm install --save @shopify/react-native-skia @react-three/fiber @react-three/native three expo-gl

# Install sensor and hardware integration packages
npm install --save expo-camera expo-face-detector expo-haptics expo-sensors

# Install AR/VR packages
npm install --save @viro-community/react-viro react-native-arkit

# Install machine learning and biometric processing
npm install --save @tensorflow/tfjs @tensorflow/tfjs-react-native

# Install database extensions for bio-cognitive data
npm install --save @supabase/postgrest-js

# Install audio processing for spatial audio
npm install --save expo-av expo-three-ar

# Add required pods for iOS (macOS only)
# cd ios
# pod install
# cd ..
